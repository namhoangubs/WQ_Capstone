Worldquant Capstone Project:
Baseline model: HMM - WGAN
Members: Co Nguyen, Hieu Ha, Nam Hoang

To install dependencies:

```bash
pip install -r requirements.txt
```

To replicate the results, run:

```bash
python p0_0_orchestration.py
```

Project paths are resolved automatically from the repository folder in `project_config.py`.
